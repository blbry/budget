import React from 'react';

function Properties() {
  // const { formatDate } = useDate();

  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold">Properties</h1>
      <br />
      <h2>Coming Soon!</h2>
      <p>(I have not yet dealt with this in real life and am thus not the person to build this out)</p>
    </div>
  );
}

export default Properties;
